var btnStart = document.querySelector('#myBtn');

var startQuiz = function() {
    btnStart.addEventListener('click', function(){
        //when button is clicked I want to start a timer using window.setTimeout(function,time)
        
    })
}

startQuiz();